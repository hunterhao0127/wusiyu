# 更新日志

## v1.5.5 macOS 更新 (2026-08-21)

### ✨ 阅读体验
- TXT 自动识别 UTF-8 / GBK / GB18030 / ANSI 编码，减少乱码
- TXT 无空行时按行分段，超长段落按句子拆分
- 阅读器按内容块渲染，支持基础图片位置保留
- 支持页码/段落跳转，阅读历史恢复更稳定

### 📕 背单词
- 背单词流程调整为：先回忆 → 点击卡片看释义 → 可查看详细解释 → 三键评分
- 复习间隔按 SM-2 思路保存 repetitions / interval / easeFactor / nextReview
- 三个评分按钮显示下次复习时间，并优化鼠标悬停/点击反馈

### 🐛 macOS
- 打包版内置 PyInstaller 后端，不要求用户手动启动 Flask
- 启动连接固定为 `127.0.0.1:5980`，避免 `localhost` 解析导致偶发白屏退出
- 后端冷启动等待时间放宽，启动失败时不再直接闪退
- 用户书籍、API Key、单词本和阅读历史保存到 macOS 应用数据目录

## 🌐 网页版 (2026-08-03)

### ✨ 新增访问方式（不影响已有 Windows / macOS / Android 版本）
- 纯前端网页版：浏览器直接访问，无需安装，任何设备可用
- 数据只存使用者自己的浏览器（IndexedDB / localStorage），API Key 直连厂商，无任何服务器存储
- 上线地址：https://hunterhao0127.github.io/wusiyu/

## v1.5.5 (2026-08-03)

### 🔧 Android 传书导入加强 + 诊断版
- 原生层每一步弹系统提示条（收到文件/已通知前端/失败原因），便于定位断点
- 通知前端重试延长到 15 秒 + 前端轮询 20 秒等待待导入文件（三重兜底）

## v1.5.4 (2026-08-03)

### 🐛 修复（Android 传书导入，三重保障）
- 冷启动 WebView 未就绪导致导入指令丢失 → 原生重试 + 前端加载后自动检查（双保险）
- 文件管理器/QQ 传书 mime 类型不匹配（octet-stream）→ intent-filter 加通配 mime，打开列表必现务思语
- 文件管理器丢失文件名/扩展名（content:// 查不到 displayName）→ 文件头探测自动修正扩展名

## v1.5.3 (2026-08-03)

### 🐛 修复（Android）
- **微信/文件管理器传书导入失败**：冷启动时 WebView 未就绪，导入指令丢失
  - 原生：等待 WebView 就绪并重试（最多 10 秒），路径先写入 `window.__pendingImportPath`
  - 前端：页面加载完成后自动检查待导入文件并导入（双保险）
- 移除 v1.5.2 有问题的 Android APK

## v1.5.2 (2026-08-03)

### 🐛 修复（Windows / Mac / Android 三端同步）
- **词组/句子划线全面修复**（4 个根因）：
  - 长文本优先匹配（句子优先，避免短词组占位导致长句跳过）
  - 引号归一化（书里弯引号 `'` 与录入直引号 `'` 视为相同）
  - 词组多次出现时刷新节点快照（之前只有第一次能划线）
  - 超长段落重排后补调高亮（之前超高段落里词组/句子不划线）
- **单词本显示完整释义**：录入不再截断 200 字符；点击词条弹出详情窗（完整释义/原文例句/来源/添加时间）
- **阅读位置保持**：加入/移除词条后保留段落内滚动位置（之前长段落会跳回顶部）
- **Windows 版**：修复前端 JS 语法错误导致「选择书籍」等按钮全部失效；后端改为无控制台窗口（任务栏不再出现两个图标）；API Key 自动填充；书库正确读取用户书籍

## v1.5.1 (2026-08-03)

### ✨ 新功能
- **支持所有 OpenAI 兼容 API 厂商**：DeepSeek / 千问 DashScope / OpenAI / Gemini / Claude / Kimi / 智谱 GLM / 自定义（任意兼容接口），全平台（Windows / Mac / Android）统一
- 平板版更名 **Android 版**（04-Android版），APK 更名 `务思语_Android.apk`
- 三端版本号统一为 v1.5.1

### 🎨 改进
- 设置面板：API 服务商下拉选择 + 自定义 Base URL / 模型名
- 切换厂商后保存并测试立即生效（无需重启）

## v1.5.0 (2026-08-03)

### ✨ 新功能
- 华为Android版 APK 发布（纯前端 + Capacitor，支持微信传书「用其他应用打开」）
- 支持 **DeepSeek** 和 **阿里千问 DashScope** 双 AI 提供商
- 内置示例书，首次启动自动导入

### 🎨 改进
- Windows 版改为 **pywebview 原生窗口**（无浏览器、无控制台）
- 标记内容三色波浪下划线 + 三套配色方案（经典/温和/极简）
- 阅读位置保持：标记/翻译后**不跳回页顶**

### 🛠️ 更新机制
- 安装程序自动检测原安装位置，原地更新，保留书籍和配置
- 修复中文路径下的 PowerShell 编码问题

## v1.4.0 (2026-07-20)

- Electron 原生窗口版发布（Windows）
- 单词本 + 背单词模式 + 记忆曲线（SM-2）
- 词组/句子高亮识别

## v1.0.0 (2026-07-19)

- 首个 Windows 版本
- TXT/EPUB/PDF/DOCX/HTML 阅读
- 点击查词 + 句子翻译
