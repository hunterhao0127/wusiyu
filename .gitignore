# ═══ 安全：密钥文件（绝不能上传） ═══
config.json
*.env
.env
**/config.json

# ═══ 依赖 ═══
node_modules/
**/node_modules/

# ═══ Python ═══
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# ═══ 构建产物目录（整个排除） ═══
build/
**/build/
dist/
**/dist/
_internal/
**/_internal/

# ═══ 二进制库文件（打包产物，不提交） ═══
*.dll
*.pyd
*.exe
*.so
*.dylib
*.zip
*.whl

# ═══ 安装程序目录：只保留脚本，排除嵌入的 exe/库/书 ═══
**/installer/*
!**/installer/*.py
!**/installer/*.bat
!**/installer/*.txt

# ═══ Electron 版的 Flask 打包副本（源码在 01/03 版） ═══
01-Windows版/flask-app/
**/flask-app/_internal/
**/flask-app/*.exe

# ═══ 安卓构建副本（大文件） ═══
03-Android版/android构建/
03-Android版/android/

# ═══ 已安装副本（Electron 安装目录，大文件） ═══
04-已安装副本/

# ═══ 系统 ═══
.DS_Store
Thumbs.db
*.log
*.tmp
*.bak

# ═══ 编辑器 ═══
.vscode/
.idea/
*.swp

# 本地备份（含敏感配置，不上传）
05-备份/

# ═══ 构建工具（SDK/Gradle/JDK，数 GB） ═══
06-构建工具/
**/06-构建工具/

# ═══ 图标设计素材 ═══
图标方案/

# ═══ 备份文件变体 ═══
*.bak*

# ═══ 用户数据保护（绝不能上传） ═══
# 书籍文件（用户导入的电子书）
*.epub
*.pdf
*.docx
*.mobi
*.azw3
**/books/*
!**/books/*.txt

# 单词本/阅读历史/用户数据
wordbook*.json
history*.json
**/data/*.json
**/data/*.db
*.sqlite
*.sqlite3

# API 密钥文件（含变体）
*api_key*
*apikey*
secrets.json

# ═══ APK 构建产物（只发 Release，不入仓库） ═══
*.apk
